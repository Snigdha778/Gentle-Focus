
  # Generate design from JSX (Copy)

  This is a code bundle for Generate design from JSX (Copy). The original project is available at https://www.figma.com/design/bfGw28ZwmCoYnymegWwgFE/Generate-design-from-JSX--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  